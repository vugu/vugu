package vugu

// TODO: doesn't work yet - but we definitely should look into how to make the hashing as
// blazingly fast as possible - needs timing.

//func xxh32(ptr uint32, length uint32, seed uint32) (result uint32)

// func init() {
// 	println(xxh32(0, 0, 0))
// }
