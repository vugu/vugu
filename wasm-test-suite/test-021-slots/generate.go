package main

// BE VERY CAREFUL
// The test relies on a generated file called "root_gen.go" which is created from the "root.go" and "root.vugu" files.
// so we MUST NOT supply the "-s" switch to the "vugugen" command
//go:generate vugugen
